package frc.robot.autos;

/** The type of autonomous action a target represents. */
public enum ActionType {
  /** Score FUEL into the HUB. */
  SCORE,
  /** Collect FUEL from the field. */
  COLLECT,
  /** Climb the TOWER for endgame points. */
  CLIMB
}
